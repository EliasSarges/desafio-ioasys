import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textContainer: {
    width: "75%",
    marginHorizontal: 20,
    flex: 2,
  },

  title: { fontSize: 22, fontWeight: "bold", marginTop: 10 },
  content: { color: "#6B6969", fontSize: 18, marginTop: 2 },
});
